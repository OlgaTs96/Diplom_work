# Автоматизация тестирования проекта

## Цель проекта
Данный проект предназначен для автоматизации тестирования веб-интерфейса и API.  
Он включает в себя автоматические тесты для проверки авторизации, создания проектов, задач и их удаления как через UI с помощью Selenium, так и через API.  
Цель — обеспечить надежность и качество продукта за счет автоматических регресс-тестов.

## Описание базового синтаксиса записи и форматирования

Проект использует следующие технологии и синтаксис:

1. **Python**: Основной язык программирования для написания тестов.
2. **Selenium**: Библиотека для автоматизации взаимодействия с веб-браузером.
3. **Pytest**: Фреймворк для написания и запуска тестов.
4. **Allure**: Инструмент для генерации отчетов о выполнении тестов.

### Форматирование кода

- Код форматируется в соответствии с PEP 8 (стиль написания кода на Python).
- Используются docstrings для документирования методов и функций.
- Все шаги теста размечаются с помощью `@allure.step` для улучшения читаемости отчетов.

---
## Инструкция по заполнению файла .env

LOGIN=your_email@example.com
PASSWORD=your_password
API_LOGIN=your_email@example.com
API_PASSWORD=your_password
BASE_URL=https://ru.yougile.com

---

## Инструкция по запуску тестов для формирования отчета Allure

1. Установите необходимые зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Запустите тесты с генерацией отчета Allure:
   ```bash
   pytest --alluredir=./allure-results
   ```

   Эта команда запустит все тесты и сохранит результаты в директорию `./allure-results`.

3. Если вы хотите запустить только ui-тесты, выполните команду 
pytest -m api --alluredir=./allure-results

Для запуска только ui-тестов, команду:
pytest -m ui --alluredir=./allure-results
---

## Инструкция по просмотру сформированного отчета Allure

1. После завершения тестов, чтобы просмотреть отчет, выполните команду:
   ```bash
   allure serve ./allure-results
   ```

   Эта команда запустит локальный сервер и откроет отчет в браузере.

2. В отчете Allure вы увидите:
   - **Название теста**: Например, `Тест: Успешная авторизация`.
   - **Описание теста**: Описание, что тест проверяет корректность работы формы авторизации.
   - **Шаги теста**:
     - Открываем страницу и авторизуемся (позитивный сценарий)
     - Кликаем кнопку входа.
     - Вводим email и пароль, подтверждаем.
     - Ожидаем перенаправления на страницу команды
     - Проверка текущего URL
   - **Результат**: Успешное выполнение или ошибка с указанием ожидаемого и фактического результата.

## Ссылка на проект
https://olga-ts.yonote.ru/share/66336c94-7499-4c0c-8934-1daf831c0ed4
=======
